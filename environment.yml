name: codex-literature
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - poppler
  - tesseract
  - pip:
      # Keep this portable when -f uses an absolute skill path from another cwd.
      # Keep versions in sync with requirements.txt.
      - pypdf>=4.3.0
      - pdfplumber>=0.11.4
      - pymupdf>=1.24.0
      - pillow>=10.4.0
      - requests>=2.32.0
      - beautifulsoup4>=4.12.0
      - selenium>=4.28.0
      - websocket-client>=1.8.0
