interface:
  display_name: "Zotero Linked Attachments"
  short_description: "Prepare and verify Zotero linked-file scripts"
  default_prompt: "Use $zotero-linked-attachments to prepare user-run linked-file scripts for local PDF or Markdown files, then verify the resulting Zotero child attachments read-only."
